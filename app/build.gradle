plugins { id 'com.android.application' }

android {
    namespace 'hr.drenovac.gp8stoperica'
    compileSdk 35

    defaultConfig {
        applicationId 'hr.drenovac.gp8stoperica'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
